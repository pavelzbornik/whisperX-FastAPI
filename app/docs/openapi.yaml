openapi: 3.1.0
info:
  title: whisperX REST service
  description: "\n    # whisperX RESTful API\n\n    Welcome to the whisperX RESTful API! This API provides a suite of audio processing services to enhance and analyze your audio content.\n\n    ## Documentation:\n\n    For detailed information on request and response formats, consult the [WhisperX Documentation](https://github.com/m-bain/whisperX).\n\n    ## Services:\n\n    Speech-2-Text provides a suite of audio processing services to enhance and analyze your audio content. The following services are available:\n\n    1. Transcribe: Transcribe an audio/video  file into text.\n    2. Align: Align the transcript to the audio/video file.\n    3. Diarize: Diarize an audio/video file into speakers.\n    4. Combine Transcript and Diarization: Combine the transcript and diarization results.\n\n    ## Supported file extensions:\n    AUDIO_EXTENSIONS = {'.aac', '.wav', '.m4a', '.ogg', '.mp3', '.oga', '.amr', '.wma', '.awb'}\n\n    VIDEO_EXTENSIONS = {'.avi', '.wmv', '.mov', '.mkv', '.mp4'}\n\n    "
  version: 0.0.1
paths:
  /speech-to-text:
    post:
      tags:
        - Speech-2-Text
      summary: Speech To Text
      description: "Process an uploaded audio file for speech-to-text conversion.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    model_params (WhisperModelParams): Whisper model parameters.\n    align_params (AlignmentParams): Alignment parameters.\n    diarize_params (DiarizationParams): Diarization parameters.\n    asr_options_params (ASROptions): ASR options parameters.\n    vad_options_params (VADOptions): VAD options parameters.\n    file (UploadFile): Uploaded audio file.\n    callback_url (str | None): Optional URL to call back when processing is complete.\n    repository (ITaskRepository): Task repository dependency.\n    file_service (FileService): File service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: speech_to_text_speech_to_text_post
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
            description: Language to transcribe
            enum:
              - en
              - zh
              - de
              - es
              - ru
              - ko
              - fr
              - ja
              - pt
              - tr
              - pl
              - ca
              - nl
              - ar
              - sv
              - it
              - id
              - hi
              - fi
              - vi
              - he
              - uk
              - el
              - ms
              - cs
              - ro
              - da
              - hu
              - ta
              - 'no'
              - th
              - ur
              - hr
              - bg
              - lt
              - la
              - mi
              - ml
              - cy
              - sk
              - te
              - fa
              - lv
              - bn
              - sr
              - az
              - sl
              - kn
              - et
              - mk
              - br
              - eu
              - is
              - hy
              - ne
              - mn
              - bs
              - kk
              - sq
              - sw
              - gl
              - mr
              - pa
              - si
              - km
              - sn
              - yo
              - so
              - af
              - oc
              - ka
              - be
              - tg
              - sd
              - gu
              - am
              - yi
              - lo
              - uz
              - fo
              - ht
              - ps
              - tk
              - nn
              - mt
              - sa
              - lb
              - my
              - bo
              - tl
              - mg
              - as
              - tt
              - haw
              - ln
              - ha
              - ba
              - jw
              - su
              - yue
            default: en
            title: Language
          description: Language to transcribe
        - name: task
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TaskEnum'
            description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
            default: transcribe
          description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
        - name: model
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/WhisperModel'
            description: Name of the Whisper model to use
            default: tiny
          description: Name of the Whisper model to use
        - name: device
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Device'
            description: Device to use for PyTorch inference
            default: cuda
          description: Device to use for PyTorch inference
        - name: device_index
          in: query
          required: false
          schema:
            type: integer
            description: Device index to use for FasterWhisper inference
            default: 0
            title: Device Index
          description: Device index to use for FasterWhisper inference
        - name: threads
          in: query
          required: false
          schema:
            type: integer
            description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
            default: 0
            title: Threads
          description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
        - name: batch_size
          in: query
          required: false
          schema:
            type: integer
            description: The preferred batch size for inference
            default: 8
            title: Batch Size
          description: The preferred batch size for inference
        - name: chunk_size
          in: query
          required: false
          schema:
            type: integer
            description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
            default: 20
            title: Chunk Size
          description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
        - name: compute_type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ComputeType'
            description: Type of computation
            default: float16
          description: Type of computation
        - name: align_model
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Name of phoneme-level ASR model to do alignment
            title: Align Model
          description: Name of phoneme-level ASR model to do alignment
        - name: interpolate_method
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/InterpolateMethod'
            description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
            default: nearest
          description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
        - name: return_char_alignments
          in: query
          required: false
          schema:
            type: boolean
            description: Return character-level alignments in the output json file
            default: false
            title: Return Char Alignments
          description: Return character-level alignments in the output json file
        - name: min_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Minimum number of speakers to in audio file
            title: Min Speakers
          description: Minimum number of speakers to in audio file
        - name: max_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Maximum number of speakers to in audio file
            title: Max Speakers
          description: Maximum number of speakers to in audio file
        - name: return_embeddings
          in: query
          required: false
          schema:
            type: boolean
            description: Include speaker embedding vectors in the task result
            default: false
            title: Return Embeddings
          description: Include speaker embedding vectors in the task result
        - name: identify_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Match speakers against the local speaker database and replace generic labels with known names
            default: false
            title: Identify Speakers
          description: Match speakers against the local speaker database and replace generic labels with known names
        - name: auto_store_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
            default: false
            title: Auto Store Speakers
          description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
        - name: beam_size
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams in beam search, only applicable when temperature is zero
            default: 5
            title: Beam Size
          description: Number of beams in beam search, only applicable when temperature is zero
        - name: best_of
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams to keep in beam search, only applicable when temperature is zero
            default: 5
            title: Best Of
          description: Number of beams to keep in beam search, only applicable when temperature is zero
        - name: patience
          in: query
          required: false
          schema:
            type: number
            description: Optional patience value to use in beam decoding
            default: 1.0
            title: Patience
          description: Optional patience value to use in beam decoding
        - name: length_penalty
          in: query
          required: false
          schema:
            type: number
            description: Optional token length penalty coefficient
            default: 1.0
            title: Length Penalty
          description: Optional token length penalty coefficient
        - name: temperatures
          in: query
          required: false
          schema:
            type: number
            description: Temperature to use for sampling
            default: 0.0
            title: Temperatures
          description: Temperature to use for sampling
        - name: compression_ratio_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the gzip compression ratio is higher than this value, treat the decoding as failed
            default: 2.4
            title: Compression Ratio Threshold
          description: If the gzip compression ratio is higher than this value, treat the decoding as failed
        - name: log_prob_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the average log probability is lower than this value, treat the decoding as failed
            default: -1.0
            title: Log Prob Threshold
          description: If the average log probability is lower than this value, treat the decoding as failed
        - name: no_speech_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
            default: 0.6
            title: No Speech Threshold
          description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
        - name: initial_prompt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Optional text to provide as a prompt for the first window.
            title: Initial Prompt
          description: Optional text to provide as a prompt for the first window.
        - name: suppress_tokens
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
            description: Comma-separated list of token ids to suppress during sampling
            default:
              - -1
            title: Suppress Tokens
          description: Comma-separated list of token ids to suppress during sampling
        - name: suppress_numerals
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: Whether to suppress numeric symbols and currency symbols during sampling
            default: false
            title: Suppress Numerals
          description: Whether to suppress numeric symbols and currency symbols during sampling
        - name: hotwords
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Hotwords related prompt applied before each transcription window
            title: Hotwords
          description: Hotwords related prompt applied before each transcription window
        - name: vad_onset
          in: query
          required: false
          schema:
            type: number
            description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
            default: 0.5
            title: Vad Onset
          description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
        - name: vad_offset
          in: query
          required: false
          schema:
            type: number
            description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
            default: 0.363
            title: Vad Offset
          description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
        - name: callback_url
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: uri
                minLength: 1
                maxLength: 2083
              - type: 'null'
            title: Callback Url
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_speech_to_text_speech_to_text_post'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /speech-to-text-url:
    post:
      tags:
        - Speech-2-Text
      summary: Speech To Text Url
      description: "Process an audio file from a URL for speech-to-text conversion.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    model_params (WhisperModelParams): Whisper model parameters.\n    align_params (AlignmentParams): Alignment parameters.\n    diarize_params (DiarizationParams): Diarization parameters.\n    asr_options_params (ASROptions): ASR options parameters.\n    vad_options_params (VADOptions): VAD options parameters.\n    url (str): URL of the audio file.\n    callback_url (str | None): Optional URL to call back when processing is complete.\n    repository (ITaskRepository): Task repository dependency.\n    file_service (FileService): File service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: speech_to_text_url_speech_to_text_url_post
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
            description: Language to transcribe
            enum:
              - en
              - zh
              - de
              - es
              - ru
              - ko
              - fr
              - ja
              - pt
              - tr
              - pl
              - ca
              - nl
              - ar
              - sv
              - it
              - id
              - hi
              - fi
              - vi
              - he
              - uk
              - el
              - ms
              - cs
              - ro
              - da
              - hu
              - ta
              - 'no'
              - th
              - ur
              - hr
              - bg
              - lt
              - la
              - mi
              - ml
              - cy
              - sk
              - te
              - fa
              - lv
              - bn
              - sr
              - az
              - sl
              - kn
              - et
              - mk
              - br
              - eu
              - is
              - hy
              - ne
              - mn
              - bs
              - kk
              - sq
              - sw
              - gl
              - mr
              - pa
              - si
              - km
              - sn
              - yo
              - so
              - af
              - oc
              - ka
              - be
              - tg
              - sd
              - gu
              - am
              - yi
              - lo
              - uz
              - fo
              - ht
              - ps
              - tk
              - nn
              - mt
              - sa
              - lb
              - my
              - bo
              - tl
              - mg
              - as
              - tt
              - haw
              - ln
              - ha
              - ba
              - jw
              - su
              - yue
            default: en
            title: Language
          description: Language to transcribe
        - name: task
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TaskEnum'
            description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
            default: transcribe
          description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
        - name: model
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/WhisperModel'
            description: Name of the Whisper model to use
            default: tiny
          description: Name of the Whisper model to use
        - name: device
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Device'
            description: Device to use for PyTorch inference
            default: cuda
          description: Device to use for PyTorch inference
        - name: device_index
          in: query
          required: false
          schema:
            type: integer
            description: Device index to use for FasterWhisper inference
            default: 0
            title: Device Index
          description: Device index to use for FasterWhisper inference
        - name: threads
          in: query
          required: false
          schema:
            type: integer
            description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
            default: 0
            title: Threads
          description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
        - name: batch_size
          in: query
          required: false
          schema:
            type: integer
            description: The preferred batch size for inference
            default: 8
            title: Batch Size
          description: The preferred batch size for inference
        - name: chunk_size
          in: query
          required: false
          schema:
            type: integer
            description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
            default: 20
            title: Chunk Size
          description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
        - name: compute_type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ComputeType'
            description: Type of computation
            default: float16
          description: Type of computation
        - name: align_model
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Name of phoneme-level ASR model to do alignment
            title: Align Model
          description: Name of phoneme-level ASR model to do alignment
        - name: interpolate_method
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/InterpolateMethod'
            description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
            default: nearest
          description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
        - name: return_char_alignments
          in: query
          required: false
          schema:
            type: boolean
            description: Return character-level alignments in the output json file
            default: false
            title: Return Char Alignments
          description: Return character-level alignments in the output json file
        - name: min_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Minimum number of speakers to in audio file
            title: Min Speakers
          description: Minimum number of speakers to in audio file
        - name: max_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Maximum number of speakers to in audio file
            title: Max Speakers
          description: Maximum number of speakers to in audio file
        - name: return_embeddings
          in: query
          required: false
          schema:
            type: boolean
            description: Include speaker embedding vectors in the task result
            default: false
            title: Return Embeddings
          description: Include speaker embedding vectors in the task result
        - name: identify_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Match speakers against the local speaker database and replace generic labels with known names
            default: false
            title: Identify Speakers
          description: Match speakers against the local speaker database and replace generic labels with known names
        - name: auto_store_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
            default: false
            title: Auto Store Speakers
          description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
        - name: beam_size
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams in beam search, only applicable when temperature is zero
            default: 5
            title: Beam Size
          description: Number of beams in beam search, only applicable when temperature is zero
        - name: best_of
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams to keep in beam search, only applicable when temperature is zero
            default: 5
            title: Best Of
          description: Number of beams to keep in beam search, only applicable when temperature is zero
        - name: patience
          in: query
          required: false
          schema:
            type: number
            description: Optional patience value to use in beam decoding
            default: 1.0
            title: Patience
          description: Optional patience value to use in beam decoding
        - name: length_penalty
          in: query
          required: false
          schema:
            type: number
            description: Optional token length penalty coefficient
            default: 1.0
            title: Length Penalty
          description: Optional token length penalty coefficient
        - name: temperatures
          in: query
          required: false
          schema:
            type: number
            description: Temperature to use for sampling
            default: 0.0
            title: Temperatures
          description: Temperature to use for sampling
        - name: compression_ratio_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the gzip compression ratio is higher than this value, treat the decoding as failed
            default: 2.4
            title: Compression Ratio Threshold
          description: If the gzip compression ratio is higher than this value, treat the decoding as failed
        - name: log_prob_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the average log probability is lower than this value, treat the decoding as failed
            default: -1.0
            title: Log Prob Threshold
          description: If the average log probability is lower than this value, treat the decoding as failed
        - name: no_speech_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
            default: 0.6
            title: No Speech Threshold
          description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
        - name: initial_prompt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Optional text to provide as a prompt for the first window.
            title: Initial Prompt
          description: Optional text to provide as a prompt for the first window.
        - name: suppress_tokens
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
            description: Comma-separated list of token ids to suppress during sampling
            default:
              - -1
            title: Suppress Tokens
          description: Comma-separated list of token ids to suppress during sampling
        - name: suppress_numerals
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: Whether to suppress numeric symbols and currency symbols during sampling
            default: false
            title: Suppress Numerals
          description: Whether to suppress numeric symbols and currency symbols during sampling
        - name: hotwords
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Hotwords related prompt applied before each transcription window
            title: Hotwords
          description: Hotwords related prompt applied before each transcription window
        - name: vad_onset
          in: query
          required: false
          schema:
            type: number
            description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
            default: 0.5
            title: Vad Onset
          description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
        - name: vad_offset
          in: query
          required: false
          schema:
            type: number
            description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
            default: 0.363
            title: Vad Offset
          description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
        - name: callback_url
          in: query
          required: false
          schema:
            anyOf:
              - type: string
                format: uri
                minLength: 1
                maxLength: 2083
              - type: 'null'
            title: Callback Url
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Body_speech_to_text_url_speech_to_text_url_post'
      callbacks:
        task_notification:
          '{$callback_url}':
            post:
              tags:
                - Callbacks
              summary: Task completion callback
              description: Webhook endpoint that receives task completion notifications
              operationId: task_completion_callback
              requestBody:
                required: true
                content:
                  application/json:
                    schema:
                      $ref: '#/components/schemas/Result'
              responses:
                '200':
                  description: Successful Response
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/TaskEventReceived'
                '422':
                  description: Validation Error
                  content:
                    application/json:
                      schema:
                        $ref: '#/components/schemas/HTTPValidationError'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /task/all:
    get:
      tags:
        - Tasks Management
      summary: Get All Tasks Status
      description: "Retrieve the status of all tasks.\n\nArgs:\n    service: Task management service dependency.\n\nReturns:\n    TaskListResponse: The status of all tasks."
      operationId: get_all_tasks_status_task_all_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskListResponse'
  /task/{identifier}:
    get:
      tags:
        - Tasks Management
      summary: Get Transcription Status
      description: "Retrieve the status of a specific task by its identifier.\n\nArgs:\n    identifier (str): The identifier of the task.\n    service: Task management service dependency.\n\nReturns:\n    Result: The status of the task.\n\nRaises:\n    TaskNotFoundError: If the identifier is not found."
      operationId: get_transcription_status_task__identifier__get
      parameters:
        - name: identifier
          in: path
          required: true
          schema:
            type: string
            title: Identifier
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Result'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /task/{identifier}/delete:
    delete:
      tags:
        - Tasks Management
      summary: Delete Task
      description: "Delete a specific task by its identifier.\n\nArgs:\n    identifier (str): The identifier of the task.\n    service: Task management service dependency.\n\nReturns:\n    Response: Confirmation message of task deletion.\n\nRaises:\n    TaskNotFoundError: If the task is not found."
      operationId: delete_task_task__identifier__delete_delete
      parameters:
        - name: identifier
          in: path
          required: true
          schema:
            type: string
            title: Identifier
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /service/transcribe:
    post:
      tags:
        - Speech-2-Text services
      summary: 1. Transcribe
      description: "Transcribe an uploaded audio file.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    model_params (WhisperModelParams): Whisper model parameters.\n    asr_options_params (ASROptions): ASR options parameters.\n    vad_options_params (VADOptions): VAD options parameters.\n    file (UploadFile): Uploaded audio file.\n    repository (ITaskRepository): Task repository dependency.\n    file_service (FileService): File service dependency.\n    transcription_service (ITranscriptionService): Transcription service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: 1__Transcribe_service_transcribe_post
      parameters:
        - name: language
          in: query
          required: false
          schema:
            type: string
            description: Language to transcribe
            enum:
              - en
              - zh
              - de
              - es
              - ru
              - ko
              - fr
              - ja
              - pt
              - tr
              - pl
              - ca
              - nl
              - ar
              - sv
              - it
              - id
              - hi
              - fi
              - vi
              - he
              - uk
              - el
              - ms
              - cs
              - ro
              - da
              - hu
              - ta
              - 'no'
              - th
              - ur
              - hr
              - bg
              - lt
              - la
              - mi
              - ml
              - cy
              - sk
              - te
              - fa
              - lv
              - bn
              - sr
              - az
              - sl
              - kn
              - et
              - mk
              - br
              - eu
              - is
              - hy
              - ne
              - mn
              - bs
              - kk
              - sq
              - sw
              - gl
              - mr
              - pa
              - si
              - km
              - sn
              - yo
              - so
              - af
              - oc
              - ka
              - be
              - tg
              - sd
              - gu
              - am
              - yi
              - lo
              - uz
              - fo
              - ht
              - ps
              - tk
              - nn
              - mt
              - sa
              - lb
              - my
              - bo
              - tl
              - mg
              - as
              - tt
              - haw
              - ln
              - ha
              - ba
              - jw
              - su
              - yue
            default: en
            title: Language
          description: Language to transcribe
        - name: task
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TaskEnum'
            description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
            default: transcribe
          description: Whether to perform X->X speech recognition ('transcribe') or X->English translation ('translate')
        - name: model
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/WhisperModel'
            description: Name of the Whisper model to use
            default: tiny
          description: Name of the Whisper model to use
        - name: device
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Device'
            description: Device to use for PyTorch inference
            default: cuda
          description: Device to use for PyTorch inference
        - name: device_index
          in: query
          required: false
          schema:
            type: integer
            description: Device index to use for FasterWhisper inference
            default: 0
            title: Device Index
          description: Device index to use for FasterWhisper inference
        - name: threads
          in: query
          required: false
          schema:
            type: integer
            description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
            default: 0
            title: Threads
          description: Number of threads used by torch for CPU inference; supersedes MKL_NUM_THREADS/OMP_NUM_THREADS
        - name: batch_size
          in: query
          required: false
          schema:
            type: integer
            description: The preferred batch size for inference
            default: 8
            title: Batch Size
          description: The preferred batch size for inference
        - name: chunk_size
          in: query
          required: false
          schema:
            type: integer
            description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
            default: 20
            title: Chunk Size
          description: Chunk size for merging VAD segments. Default is 20, reduce this if the chunk is too long.
        - name: compute_type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/ComputeType'
            description: Type of computation
            default: float16
          description: Type of computation
        - name: beam_size
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams in beam search, only applicable when temperature is zero
            default: 5
            title: Beam Size
          description: Number of beams in beam search, only applicable when temperature is zero
        - name: best_of
          in: query
          required: false
          schema:
            type: integer
            description: Number of beams to keep in beam search, only applicable when temperature is zero
            default: 5
            title: Best Of
          description: Number of beams to keep in beam search, only applicable when temperature is zero
        - name: patience
          in: query
          required: false
          schema:
            type: number
            description: Optional patience value to use in beam decoding
            default: 1.0
            title: Patience
          description: Optional patience value to use in beam decoding
        - name: length_penalty
          in: query
          required: false
          schema:
            type: number
            description: Optional token length penalty coefficient
            default: 1.0
            title: Length Penalty
          description: Optional token length penalty coefficient
        - name: temperatures
          in: query
          required: false
          schema:
            type: number
            description: Temperature to use for sampling
            default: 0.0
            title: Temperatures
          description: Temperature to use for sampling
        - name: compression_ratio_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the gzip compression ratio is higher than this value, treat the decoding as failed
            default: 2.4
            title: Compression Ratio Threshold
          description: If the gzip compression ratio is higher than this value, treat the decoding as failed
        - name: log_prob_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the average log probability is lower than this value, treat the decoding as failed
            default: -1.0
            title: Log Prob Threshold
          description: If the average log probability is lower than this value, treat the decoding as failed
        - name: no_speech_threshold
          in: query
          required: false
          schema:
            type: number
            description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
            default: 0.6
            title: No Speech Threshold
          description: If the probability of the token is higher than this value AND the decoding has failed due to `logprob_threshold`, consider the segment as silence
        - name: initial_prompt
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Optional text to provide as a prompt for the first window.
            title: Initial Prompt
          description: Optional text to provide as a prompt for the first window.
        - name: suppress_tokens
          in: query
          required: false
          schema:
            type: array
            items:
              type: integer
            description: Comma-separated list of token ids to suppress during sampling
            default:
              - -1
            title: Suppress Tokens
          description: Comma-separated list of token ids to suppress during sampling
        - name: suppress_numerals
          in: query
          required: false
          schema:
            anyOf:
              - type: boolean
              - type: 'null'
            description: Whether to suppress numeric symbols and currency symbols during sampling
            default: false
            title: Suppress Numerals
          description: Whether to suppress numeric symbols and currency symbols during sampling
        - name: hotwords
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Hotwords related prompt applied before each transcription window
            title: Hotwords
          description: Hotwords related prompt applied before each transcription window
        - name: vad_onset
          in: query
          required: false
          schema:
            type: number
            description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
            default: 0.5
            title: Vad Onset
          description: Onset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected
        - name: vad_offset
          in: query
          required: false
          schema:
            type: number
            description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
            default: 0.363
            title: Vad Offset
          description: Offset threshold for VAD (see pyannote.audio), reduce this if speech is not being detected.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_1__Transcribe_service_transcribe_post'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /service/align:
    post:
      tags:
        - Speech-2-Text services
      summary: 2. Align Transcript
      description: "Align a transcript with an audio file.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    transcript (UploadFile): Uploaded transcript file.\n    file (UploadFile): Uploaded audio file.\n    device (Device): Device for PyTorch inference.\n    align_params (AlignmentParams): Alignment parameters.\n    repository (ITaskRepository): Task repository dependency.\n    file_service (FileService): File service dependency.\n    alignment_service (IAlignmentService): Alignment service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: 2__Align_Transcript_service_align_post
      parameters:
        - name: device
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Device'
            description: Device to use for PyTorch inference
            default: cuda
          description: Device to use for PyTorch inference
        - name: align_model
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Name of phoneme-level ASR model to do alignment
            title: Align Model
          description: Name of phoneme-level ASR model to do alignment
        - name: interpolate_method
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/InterpolateMethod'
            description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
            default: nearest
          description: For word .srt, method to assign timestamps to non-aligned words, or merge them into neighboring.
        - name: return_char_alignments
          in: query
          required: false
          schema:
            type: boolean
            description: Return character-level alignments in the output json file
            default: false
            title: Return Char Alignments
          description: Return character-level alignments in the output json file
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_2__Align_Transcript_service_align_post'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /service/diarize:
    post:
      tags:
        - Speech-2-Text services
      summary: 3. Diarize
      description: "Perform diarization on an uploaded audio file.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    file (UploadFile): Uploaded audio file.\n    repository (ITaskRepository): Task repository dependency.\n    device (Device): Device for PyTorch inference.\n    diarize_params (DiarizationParams): Diarization parameters.\n    file_service (FileService): File service dependency.\n    diarization_service (IDiarizationService): Diarization service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: 3__Diarize_service_diarize_post
      parameters:
        - name: device
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/Device'
            description: Device to use for PyTorch inference
            default: cuda
          description: Device to use for PyTorch inference
        - name: min_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Minimum number of speakers to in audio file
            title: Min Speakers
          description: Minimum number of speakers to in audio file
        - name: max_speakers
          in: query
          required: false
          schema:
            anyOf:
              - type: integer
              - type: 'null'
            description: Maximum number of speakers to in audio file
            title: Max Speakers
          description: Maximum number of speakers to in audio file
        - name: return_embeddings
          in: query
          required: false
          schema:
            type: boolean
            description: Include speaker embedding vectors in the task result
            default: false
            title: Return Embeddings
          description: Include speaker embedding vectors in the task result
        - name: identify_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Match speakers against the local speaker database and replace generic labels with known names
            default: false
            title: Identify Speakers
          description: Match speakers against the local speaker database and replace generic labels with known names
        - name: auto_store_speakers
          in: query
          required: false
          schema:
            type: boolean
            description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
            default: false
            title: Auto Store Speakers
          description: Automatically store new speaker embeddings in the database (embeddings are computed automatically when enabled)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_3__Diarize_service_diarize_post'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /service/combine:
    post:
      tags:
        - Speech-2-Text services
      summary: 4. Combine Transcript And Diarization Result
      description: "Combine a transcript with diarization results.\n\nArgs:\n    background_tasks (BackgroundTasks): Background tasks dependency.\n    aligned_transcript (UploadFile): Uploaded aligned transcript file.\n    diarization_result (UploadFile): Uploaded diarization result file.\n    repository (ITaskRepository): Task repository dependency.\n    file_service (FileService): File service dependency.\n    speaker_service (ISpeakerAssignmentService): Speaker assignment service dependency.\n\nReturns:\n    Response: Confirmation message of task queuing."
      operationId: 4__Combine_Transcript_and_Diarization_result_service_combine_post
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Body_4__Combine_Transcript_and_Diarization_result_service_combine_post'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /speakers:
    post:
      tags:
        - Speakers
      summary: Create Speaker
      description: Create a new speaker embedding.
      operationId: Create_speaker_speakers_post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSpeakerRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
        - Speakers
      summary: List Speakers
      description: List speaker embeddings with optional task filter and pagination.
      operationId: List_speakers_speakers_get
      parameters:
        - name: task_id
          in: query
          required: false
          schema:
            anyOf:
              - type: string
              - type: 'null'
            description: Filter by originating task UUID
            title: Task Id
          description: Filter by originating task UUID
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 1000
            minimum: 1
            default: 100
            title: Limit
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
            title: Offset
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SpeakerResponse'
                title: Response List Speakers Speakers Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Speakers
      summary: Delete Speakers By Task
      description: Delete all speaker embeddings associated with a task.
      operationId: Delete_speakers_by_task_speakers_delete
      parameters:
        - name: task_id
          in: query
          required: true
          schema:
            type: string
            description: Delete all speakers from this task
            title: Task Id
          description: Delete all speakers from this task
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /speakers/{uuid}:
    get:
      tags:
        - Speakers
      summary: Get Speaker
      description: Get a single speaker embedding by UUID.
      operationId: Get_speaker_speakers__uuid__get
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            title: Uuid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeakerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
        - Speakers
      summary: Update Speaker
      description: Update a speaker's label, description, or embedding.
      operationId: Update_speaker_speakers__uuid__put
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            title: Uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSpeakerRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
        - Speakers
      summary: Delete Speaker
      description: Delete a single speaker embedding.
      operationId: Delete_speaker_speakers__uuid__delete
      parameters:
        - name: uuid
          in: path
          required: true
          schema:
            type: string
            title: Uuid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /speakers/search:
    post:
      tags:
        - Speakers
      summary: Search Speakers
      description: Search for similar speakers by embedding vector (cosine similarity).
      operationId: Search_speakers_speakers_search_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeakerSearchRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeakerSearchResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /speakers/identify:
    post:
      tags:
        - Speakers
      summary: Identify Speaker
      description: Identify the best-matching speaker above threshold.
      operationId: Identify_speaker_speakers_identify_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeakerIdentifyRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /health:
    get:
      tags:
        - Health
      summary: Simple health check
      description: 'Verify the service is up and running.


        Returns a simple status response to confirm the API service is operational.'
      operationId: health_check_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  /health/live:
    get:
      tags:
        - Health
      summary: Liveness check
      description: 'Check if the application is running.


        Used by orchestration systems like Kubernetes to detect if the app is alive.

        Returns timestamp along with status information.'
      operationId: liveness_check_health_live_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
  /health/ready:
    get:
      tags:
        - Health
      summary: Readiness check
      description: 'Check if the application is ready to accept requests.


        Verifies dependencies like the database are connected and ready.

        Returns HTTP 200 if all systems are operational, HTTP 503 if any dependency

        has failed.'
      operationId: readiness_check_health_ready_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
components:
  schemas:
    Body_1__Transcribe_service_transcribe_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: Audio/video file to transcribe
      type: object
      required:
        - file
      title: Body_1__Transcribe_service_transcribe_post
    Body_2__Align_Transcript_service_align_post:
      properties:
        transcript:
          type: string
          contentMediaType: application/octet-stream
          title: Transcript
          description: Whisper style transcript json file
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
          description: Audio/video file which has been transcribed
      type: object
      required:
        - transcript
        - file
      title: Body_2__Align_Transcript_service_align_post
    Body_3__Diarize_service_diarize_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
      type: object
      required:
        - file
      title: Body_3__Diarize_service_diarize_post
    Body_4__Combine_Transcript_and_Diarization_result_service_combine_post:
      properties:
        aligned_transcript:
          type: string
          contentMediaType: application/octet-stream
          title: Aligned Transcript
        diarization_result:
          type: string
          contentMediaType: application/octet-stream
          title: Diarization Result
      type: object
      required:
        - aligned_transcript
        - diarization_result
      title: Body_4__Combine_Transcript_and_Diarization_result_service_combine_post
    Body_speech_to_text_speech_to_text_post:
      properties:
        file:
          type: string
          contentMediaType: application/octet-stream
          title: File
      type: object
      required:
        - file
      title: Body_speech_to_text_speech_to_text_post
    Body_speech_to_text_url_speech_to_text_url_post:
      properties:
        url:
          type: string
          title: Url
      type: object
      required:
        - url
      title: Body_speech_to_text_url_speech_to_text_url_post
    ComputeType:
      type: string
      enum:
        - float16
        - float32
        - int8
      title: ComputeType
      description: Enum for compute types.
    CreateSpeakerRequest:
      properties:
        speaker_label:
          type: string
          title: Speaker Label
          description: User-facing name for the speaker
        embedding:
          items:
            type: number
          type: array
          title: Embedding
          description: Speaker embedding vector
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
          description: Free-text description (role, voice notes, etc.)
        task_uuid:
          anyOf:
            - type: string
            - type: 'null'
          title: Task Uuid
          description: Optional link to originating diarization task
      type: object
      required:
        - speaker_label
        - embedding
      title: CreateSpeakerRequest
      description: Request schema for creating a speaker embedding.
    Device:
      type: string
      enum:
        - cuda
        - cpu
      title: Device
      description: Enum for device types.
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    InterpolateMethod:
      type: string
      enum:
        - nearest
        - linear
        - ignore
      title: InterpolateMethod
      description: Enum for interpolation methods.
    Metadata:
      properties:
        task_type:
          type: string
          title: Task Type
        task_params:
          anyOf:
            - additionalProperties: true
              type: object
            - type: 'null'
          title: Task Params
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
        file_name:
          anyOf:
            - type: string
            - type: 'null'
          title: File Name
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
        callback_url:
          anyOf:
            - type: string
            - type: 'null'
          title: Callback Url
        duration:
          anyOf:
            - type: number
            - type: 'null'
          title: Duration
        audio_duration:
          anyOf:
            - type: number
            - type: 'null'
          title: Audio Duration
        start_time:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Start Time
        end_time:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: End Time
      type: object
      required:
        - task_type
        - task_params
        - language
        - file_name
        - url
        - callback_url
        - duration
      title: Metadata
      description: Metadata model for task information.
    Response:
      properties:
        identifier:
          type: string
          title: Identifier
        message:
          type: string
          title: Message
      type: object
      required:
        - identifier
        - message
      title: Response
      description: Response model for API responses.
    Result:
      properties:
        status:
          type: string
          title: Status
        result:
          title: Result
        metadata:
          $ref: '#/components/schemas/Metadata'
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
      type: object
      required:
        - status
        - result
        - metadata
        - error
      title: Result
      description: Model for a result with status, result data, metadata, and optional error.
    SpeakerIdentifyRequest:
      properties:
        embedding:
          items:
            type: number
          type: array
          title: Embedding
          description: Query embedding vector
        threshold:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Threshold
          description: Minimum cosine similarity threshold
          default: 0.7
      type: object
      required:
        - embedding
      title: SpeakerIdentifyRequest
      description: Request schema for identifying a speaker.
    SpeakerResponse:
      properties:
        uuid:
          type: string
          title: Uuid
        task_uuid:
          anyOf:
            - type: string
            - type: 'null'
          title: Task Uuid
        speaker_label:
          type: string
          title: Speaker Label
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
        embedding:
          items:
            type: number
          type: array
          title: Embedding
        created_at:
          type: string
          format: date-time
          title: Created At
      type: object
      required:
        - uuid
        - task_uuid
        - speaker_label
        - description
        - embedding
        - created_at
      title: SpeakerResponse
      description: Response schema for a speaker embedding.
    SpeakerSearchRequest:
      properties:
        embedding:
          items:
            type: number
          type: array
          title: Embedding
          description: Query embedding vector
        limit:
          type: integer
          maximum: 100.0
          minimum: 1.0
          title: Limit
          description: Max results to return
          default: 5
        threshold:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Threshold
          description: Minimum cosine similarity threshold
          default: 0.7
      type: object
      required:
        - embedding
      title: SpeakerSearchRequest
      description: Request schema for searching similar speakers.
    SpeakerSearchResponse:
      properties:
        results:
          items:
            $ref: '#/components/schemas/SpeakerSearchResult'
          type: array
          title: Results
      type: object
      required:
        - results
      title: SpeakerSearchResponse
      description: Response schema for speaker search.
    SpeakerSearchResult:
      properties:
        speaker:
          $ref: '#/components/schemas/SpeakerResponse'
        similarity:
          type: number
          title: Similarity
      type: object
      required:
        - speaker
        - similarity
      title: SpeakerSearchResult
      description: A single search result with similarity score.
    TaskEnum:
      type: string
      enum:
        - transcribe
        - translate
      title: TaskEnum
      description: Enum for task types.
    TaskEventReceived:
      properties:
        ok:
          type: boolean
          title: Ok
      type: object
      required:
        - ok
      title: TaskEventReceived
      description: Confirmation that the callback was received.
    TaskListResponse:
      properties:
        tasks:
          items:
            $ref: '#/components/schemas/TaskSummaryResponse'
          type: array
          title: Tasks
          description: List of task summaries
      type: object
      required:
        - tasks
      title: TaskListResponse
      description: DTO for returning a list of task summaries.
    TaskSummaryResponse:
      properties:
        identifier:
          type: string
          title: Identifier
          description: Unique identifier for the task
        status:
          type: string
          title: Status
          description: Current status of the task
        task_type:
          type: string
          title: Task Type
          description: Type of task
        file_name:
          anyOf:
            - type: string
            - type: 'null'
          title: File Name
          description: Name of the file
        url:
          anyOf:
            - type: string
            - type: 'null'
          title: Url
          description: URL of the file
        audio_duration:
          anyOf:
            - type: number
            - type: 'null'
          title: Audio Duration
          description: Duration of the audio
        language:
          anyOf:
            - type: string
            - type: 'null'
          title: Language
          description: Language of the audio
        error:
          anyOf:
            - type: string
            - type: 'null'
          title: Error
          description: Error message if task failed
        duration:
          anyOf:
            - type: number
            - type: 'null'
          title: Duration
          description: Execution duration in seconds
        start_time:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: Start Time
          description: Task start time
        end_time:
          anyOf:
            - type: string
              format: date-time
            - type: 'null'
          title: End Time
          description: Task end time
      type: object
      required:
        - identifier
        - status
        - task_type
      title: TaskSummaryResponse
      description: 'DTO for returning task summary in list operations.


        Used for GET /task/all endpoint to provide a lighter response.'
    UpdateSpeakerRequest:
      properties:
        speaker_label:
          anyOf:
            - type: string
            - type: 'null'
          title: Speaker Label
          description: Updated speaker name
        description:
          anyOf:
            - type: string
            - type: 'null'
          title: Description
          description: Updated description
        embedding:
          anyOf:
            - items:
                type: number
              type: array
            - type: 'null'
          title: Embedding
          description: Updated embedding vector
      type: object
      title: UpdateSpeakerRequest
      description: Request schema for updating a speaker embedding.
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
              - type: string
              - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
        - loc
        - msg
        - type
      title: ValidationError
    WhisperModel:
      type: string
      enum:
        - tiny
        - tiny.en
        - base
        - base.en
        - small
        - small.en
        - medium
        - medium.en
        - large
        - large-v1
        - large-v2
        - large-v3
        - large-v3-turbo
        - distil-large-v2
        - distil-medium.en
        - distil-small.en
        - distil-large-v3
        - nyrahealth/faster_CrisperWhisper
      title: WhisperModel
      description: Enum for Whisper model types.
tags:
  - name: Speech-2-Text
    description: Operations related to transcript
  - name: Speech-2-Text services
    description: Individual services for transcript
  - name: Tasks Management
    description: Manage tasks.
  - name: Health
    description: Health check endpoints to monitor application status
